<%= form_for @user_evento do |f| %>
      <div class="form-group">
        <%= f.label :Nombre_user_evento %>
        <%= f.text_field :nombre_ev %>
      </div>

    <%= f.submit 'Enviar', :class => 'btn btn-primary' %>
<% end %>