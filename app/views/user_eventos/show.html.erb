<td><%= link_to 'user_eventos', user_eventos_url %></td>
<table>
  <tr>
    <th>Nombre user_evento</th>
    <th>Topico</th>
  </tr>

    <tr>
      <td><%= @user_evento.nombre_ev %></td>
      <td><%= link_to 'Info', "http://#{@user_evento.url}" %></td>
      <td><%= link_to 'Editar', edit_user_evento_path(@user_evento.id) %></td>
    </tr>
    
</table>
