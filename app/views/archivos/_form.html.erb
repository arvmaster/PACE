<%= form_for @archivo do |f| %>
  <% if @archivo.errors.any? %>
    <div id="error_explanation">
      <h2>No se pudo realizar la acción: <%= pluralize(@archivo.errors.count, "Error/es") %> </h2>
      <ul>
        <% @archivo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>

  <% end %>
      <div class="form-group">
        <%= f.label :Nombre_archivo %>
        <%= f.text_field :nombre_archivo %>
      </div>

      <div class="form-group">
        <%= f.label :Topico %>
        <%= f.text_field :topico %>
      </div>

      <div class="form-group">
        <%= f.label :Nombre_Asignatura %>
        <%= f.text_field :asignatura %>
      </div>

      <div class="form-group">
      <%= f.label :Documento %>
      <%= f.file_field :file%>
      </div>

    <%= f.submit 'Enviar', :class => 'btn btn-primary' %>
<% end %>